export function problem(lines) {
    const n = lines[0]
    const frase = "life is not a problem to be solved"
    const fraseMaiusc = frase.toUpperCase()

    console.log(fraseMaiusc.substring(0, n))

    // life is not a problem to be solved
}